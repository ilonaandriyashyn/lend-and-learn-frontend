export enum Paths {
  Homepage = '/',
  Login = '/login',
  Dashboard = '/dashboard',
  Devices = '/devices',
  IncomingReservations = '/incoming-reservations',
  OutcomingReservations = '/outcoming-reservations',
  Profile = '/profile/:username',
  DeviceProfile = '/devices/:id',
}
